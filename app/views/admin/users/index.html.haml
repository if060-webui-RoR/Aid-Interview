%h1 Interviewers

= link_to "All Users", :action => "index"

%table.table-bordered
  %thead
    %tr
      %th First Name
      %th Last Name
      %th Email
      %th Approved
      %th Admin
  
  %tbody
    - @users.each do |user|
      %tr
        %td= user.first_name
        %td= user.last_name
        %td= user.email
        %td= user.approved? ? (raw "&#10004;") : ''
        %td= user.admin? ? (raw "&#10004;") : ''