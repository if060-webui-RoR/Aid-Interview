- param = target_level_param(interview.target_level)
.progress
  .progress-bar class = "progress-bar-#{param[:color]}" aria-valuemax="100" role="progressbar" style="width:#{param[:width]}"
  	= "#{param[:level]}"
