nav#head.navbar.navbar-default.navbar-fixed-top.topnav[role="navigation"]
  .container.topnav
    .navbar-header
      = link_to "AID-Interview", authenticated_root_path, class: "navbar-brand topnav"
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      ul.nav.navbar-nav.navbar-right
        li
          = link_to "Home", authenticated_root_path
        li
          a[href="#help"]
            | Help
        - if user_signed_in?
          li
            = link_to('Logout', destroy_user_session_path, :method => :delete)
        - else
          li
            = link_to('Login', new_user_session_path)